# ChronoSight — Frontend

React + TypeScript + Vite frontend for ChronoSight.

See the [root README](../README.md) for full setup and usage instructions.

## Dev server

```bash
npm install
npm run dev        # http://localhost:5173
```

Requires the backend running on `http://localhost:8000` (proxied automatically via Vite).
